// SPDX-License-Identifier: GPL-3.0-only
//
// DO NOT EDIT!
//
// Generated from cmd_shell.shell.cpp.in
//
// $SHELL implementation for unix

#include <config/cmd_shell.h>

#include <cerrno>
#include <chrono>
#include <cstdlib>
#include <string>
#include <sys/wait.h>
#include <thread>
#include <unistd.h>

namespace id::config
{

static int s_cmd_shell_error{};

int get_cmd_shell_error()
{
    return s_cmd_shell_error;
}

std::string cmd_shell_command()
{
    const char *shell = std::getenv("SHELL");
    if (shell == nullptr)
    {
        shell = "sh";
    }
    return shell;
}

bool cmd_shell(TimeoutCallback *timeout)
{
    s_cmd_shell_error = 0;
    const std::string shell{cmd_shell_command()};
    const pid_t child{fork()};
    if (child < 0)
    {
        s_cmd_shell_error = errno;
        return false;
    }
    if (child == 0)
    {
        execl(shell.c_str(), shell.c_str(), static_cast<char *>(nullptr));
        std::_Exit(127);
    }

    int status{};
    while (true)
    {
        const pid_t result{waitpid(child, &status, WNOHANG)};
        if (result == child)
        {
            if (WIFEXITED(status) && WEXITSTATUS(status) == 127)
            {
                s_cmd_shell_error = ENOENT;
                return false;
            }
            return true;
        }
        if (result < 0)
        {
            if (errno == EINTR)
            {
                continue;
            }
            s_cmd_shell_error = errno;
            return false;
        }
        if (timeout != nullptr)
        {
            timeout();
        }
        std::this_thread::sleep_for(std::chrono::milliseconds(100));
    }
}

} // namespace id::config
