# SPDX-License-Identifier: GPL-3.0-only
#
name: analysis

permissions:
  packages: write

on:
  push:
    branches: [ "master", "develop" ]
  pull_request:
    branches: [ "master" ]

env:
  FEED_URL: https://nuget.pkg.github.com/${{github.repository_owner}}/index.json

jobs:
  #############################################################################
  msvc-static-analysis:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683       # v4.2.2
        with:
          submodules: true

      - name: Add NuGet sources
        shell: bash
        run: |
          nuget \
            sources add \
            -Source "${{env.FEED_URL}}" \
            -StorePasswordInClearText \
            -Name GitHubPackages \
            -UserName "${{github.repository_owner}}" \
            -Password "${{secrets.GH_PACKAGES_TOKEN}}"
          nuget \
            setapikey "${{secrets.GH_PACKAGES_TOKEN}}" \
            -Source "${{env.FEED_URL}}"

      - name: Setup CMake
        uses: jwlawson/actions-setup-cmake@802fa1a2c4e212495c05bf94dba2704a92a472be # v2.0.2
        with:
          cmake-version: '3.31'

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@6fb02220983dee41ce7ae257b6f4d8f9bf5ed4ce # v2.0.0

      - name: Configure CMake for Static Analysis
        env: 
          VCPKG_BINARY_SOURCES: "clear;nuget,${{env.FEED_URL}},readwrite"
        run: |
          cmake --version
          cmake --preset ci-debug

      - name: Run Static Analysis
        continue-on-error: true
        run: |
          msbuild build-ci-debug/ALL_BUILD.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:EnableClangTidyCodeAnalysis=true /p:RunCodeAnalysis=true /p:CodeAnalysisRuleSet=AllRules.ruleset /p:TreatWarningsAsErrors=false /v:q "/flp:logfile=msvc-static-analysis-report.txt;verbosity=q"

      - name: Upload Static Analysis Report
        uses: actions/upload-artifact@c7d193f32edcb7bfad88892161225aeda64e9392 # v4.0.0
        with:
          name: msvc-static-analysis
          path: |
            msvc-static-analysis-report.txt

  #############################################################################
  cppcheck:
    name: cppcheck
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683       # v4.2.2
        with:
          submodules: true

      - name: Install System Dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          ${{github.workspace}}/vcpkg/bootstrap-vcpkg.sh
          echo "man-db man-db/auto-update boolean false" | sudo debconf-set-selections
          sudo dpkg-reconfigure man-db
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y mono-complete

      - name: Add NuGet sources
        shell: bash
        run: |
          mono `./vcpkg/vcpkg fetch nuget | tail -n 1` \
            sources add \
            -Source "${{env.FEED_URL}}" \
            -StorePasswordInClearText \
            -Name GitHubPackages \
            -UserName "${{github.repository_owner}}" \
            -Password "${{secrets.GH_PACKAGES_TOKEN}}"
          mono `./vcpkg/vcpkg fetch nuget | tail -n 1` \
            setapikey "${{secrets.GH_PACKAGES_TOKEN}}" \
            -Source "${{env.FEED_URL}}"

      - name: Setup CMake
        uses: jwlawson/actions-setup-cmake@802fa1a2c4e212495c05bf94dba2704a92a472be # v2.0.2
        with:
          cmake-version: '3.31'

      - name: Configure Compilation Database
        env: 
          VCPKG_BINARY_SOURCES: "clear;nuget,${{env.FEED_URL}},readwrite"
        run: |
          cmake --version
          cmake --preset cppcheck
  
      - name: Run CppCheck
        uses: deep5050/cppcheck-action@b4d7c69b911c24817691b4b210586568a858b4c5 # v3.0
        with:
          github_token: ${{secrets.GH_PACKAGES_TOKEN}}
          enable: 'style'
          force: 'enable'
          other_options: >-
            --library=googletest
            --suppress=commaSeparatedReturn
            --suppress=cstyleCast
            --suppress=functionConst
            --suppress=functionStatic
            --suppress=incorrectStringBooleanError
            --suppress=invalidPointerCast
            --suppress=invalidscanf
            --suppress=variableHidingEnum
            --suppress=variableScope
            --inline-suppr
            -I headers hc common headers unix win32
            -ilegacy
            -ivcpkg
            -i../build-cppcheck/vcpkg_installed

      - name: Print Report
        run: cmake -E cat cppcheck_report.txt

      - name: Upload Report
        uses: actions/upload-artifact@c7d193f32edcb7bfad88892161225aeda64e9392 # v4.0.0
        with:
          name: CppCheck
          path: cppcheck_report.txt

  #############################################################################
  include-what-you-use:
    name: include-what-you-use
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683       # v4.2.2
        with:
          submodules: true

      - name: Install System Dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          ${{github.workspace}}/vcpkg/bootstrap-vcpkg.sh
          echo "man-db man-db/auto-update boolean false" | sudo debconf-set-selections
          sudo dpkg-reconfigure man-db
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y mono-complete

      - name: Add NuGet sources
        shell: bash
        run: |
          mono `./vcpkg/vcpkg fetch nuget | tail -n 1` \
            sources add \
            -Source "${{env.FEED_URL}}" \
            -StorePasswordInClearText \
            -Name GitHubPackages \
            -UserName "${{github.repository_owner}}" \
            -Password "${{secrets.GH_PACKAGES_TOKEN}}"
          mono `./vcpkg/vcpkg fetch nuget | tail -n 1` \
            setapikey "${{secrets.GH_PACKAGES_TOKEN}}" \
            -Source "${{env.FEED_URL}}"

      - name: Setup CMake
        uses: jwlawson/actions-setup-cmake@802fa1a2c4e212495c05bf94dba2704a92a472be # v2.0.2
        with:
          cmake-version: '3.31'

      - name: Configure Compilation Database
        env: 
          VCPKG_BINARY_SOURCES: "clear;nuget,${{env.FEED_URL}},readwrite"
        run: |
          cmake --version
          cmake --preset cppcheck

      - name: Run include-what-you-use
        uses: EmilGedda/include-what-you-use-action@v1.0
        with:
          compilation-database-path: ./build-cppcheck
          output-format: iwyu
          no-error: true

      - name: Generate IWYU Report
        run: |
          echo "Include What You Use Analysis Report" > iwyu_report.txt
          echo "====================================" >> iwyu_report.txt
          echo "Generated on: $(date)" >> iwyu_report.txt
          echo "" >> iwyu_report.txt
          
          # The EmilGedda action outputs to standard output, capture any output files
          if [ -f "iwyu_output.txt" ]; then
            cat iwyu_output.txt >> iwyu_report.txt
          elif [ -f "iwyu_report.txt" ]; then
            # If the action already created a report file, use it
            echo "IWYU analysis completed." >> iwyu_report.txt
          else
            echo "IWYU analysis completed successfully." >> iwyu_report.txt
            echo "Check the action logs for detailed output." >> iwyu_report.txt
          fi

      - name: Upload Report
        uses: actions/upload-artifact@c7d193f32edcb7bfad88892161225aeda64e9392 # v4.0.0
        with:
          name: include-what-you-use
          path: iwyu_report.txt

  #############################################################################
  doxygen:
    name: doxygen
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683       # v4.2.2
        with:
          submodules: true

      - name: Install Doxygen
        run: |
          ${{github.workspace}}/vcpkg/bootstrap-vcpkg.sh
          echo "man-db man-db/auto-update boolean false" | sudo debconf-set-selections
          sudo dpkg-reconfigure man-db
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y doxygen graphviz

      - name: Setup CMake
        uses: jwlawson/actions-setup-cmake@802fa1a2c4e212495c05bf94dba2704a92a472be # v2.0.2
        with:
          cmake-version: '3.31'

      - name: Configure Project
        run: |
          cmake --version
          cmake --preset cppcheck

      - name: Get Project Version
        id: version
        run: |
          PROJECT_VERSION=$(grep -E "^\s*VERSION\s+[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+" CMakeLists.txt | sed -E 's/^\s*VERSION\s+([0-9]+\.[0-9]+\.[0-9]+\.[0-9]+).*/\1/')
          echo "version=${PROJECT_VERSION}" >> $GITHUB_OUTPUT

      - name: Generate Documentation
        run: |
          # Replace version placeholder in Doxyfile
          sed "s/@PROJECT_VERSION@/${{steps.version.outputs.version}}/g" Doxyfile > Doxyfile.configured
          doxygen Doxyfile.configured

      - name: Upload Documentation
        uses: actions/upload-artifact@c7d193f32edcb7bfad88892161225aeda64e9392 # v4.0.0
        with:
          name: doxygen
          path: docs/html/

  #############################################################################
  gh-pages-cppcheck:
    needs: cppcheck
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683       # v4.2.2
        with:
          ref: gh-pages
          submodules: false

      - name: Clear Directory
        run: rm -rf dev/cppcheck 

      - name: Get Report
        uses: actions/download-artifact@7a1cd3216ca9260cd8022db641d960b1db4d1be4 # v4.0.0
        with:
          name: CppCheck
          path: dev/cppcheck

      - name: Update Contents
        run: |
          git config user.name CppCheck
          git config user.email cppcheck-bot@example.com
          git status
          git add dev/cppcheck
          git status
          git commit -m "Update CppCheck report for ${{github.sha}}" || echo "No changes to commit"
          git status
          git log -n 1

      - name: Push Updates
        uses: ad-m/github-push-action@77c5b412c50b723d2a4fbc6d71fb5723bcd439aa # v1.0.0
        with:
          github_token: ${{secrets.GITHUB_TOKEN}}
          branch: gh-pages
          force: false

  #############################################################################
  gh-pages-msvc-static-analysis:
    needs: msvc-static-analysis
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683       # v4.2.2
        with:
          ref: gh-pages
          submodules: false

      - name: Clear Directory
        run: rm -rf dev/msvc-static-analysis

      - name: Get Report
        uses: actions/download-artifact@7a1cd3216ca9260cd8022db641d960b1db4d1be4 # v4.0.0
        with:
          name: msvc-static-analysis
          path: dev/msvc-static-analysis

      - name: Update Contents
        run: |
          git config user.name "MSVC Static Analysis"
          git config user.email msvc-static-analysis-bot@example.com
          git status
          git add dev/msvc-static-analysis
          git status
          git commit -m "Update MSVC Static Analysis report for ${{github.sha}}" || echo "No changes to commit"
          git status
          git log -n 1

      - name: Push Updates
        uses: ad-m/github-push-action@77c5b412c50b723d2a4fbc6d71fb5723bcd439aa # v1.0.0
        with:
          github_token: ${{secrets.GITHUB_TOKEN}}
          branch: gh-pages
          force: false

  #############################################################################
  gh-pages-include-what-you-use:
    needs: include-what-you-use
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683       # v4.2.2
        with:
          ref: gh-pages
          submodules: false

      - name: Clear Directory
        run: rm -rf dev/include-what-you-use

      - name: Get Report
        uses: actions/download-artifact@7a1cd3216ca9260cd8022db641d960b1db4d1be4 # v4.0.0
        with:
          name: include-what-you-use
          path: dev/include-what-you-use

      - name: Update Contents
        run: |
          git config user.name "Include What You Use"
          git config user.email iwyu-bot@example.com
          git status
          git add dev/include-what-you-use
          git status
          git commit -m "Update Include What You Use report for ${{github.sha}}" || echo "No changes to commit"
          git status
          git log -n 1

      - name: Push Updates
        uses: ad-m/github-push-action@77c5b412c50b723d2a4fbc6d71fb5723bcd439aa # v1.0.0
        with:
          github_token: ${{secrets.GITHUB_TOKEN}}
          branch: gh-pages
          force: false

  #############################################################################
  gh-pages-doxygen:
    needs: doxygen
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683       # v4.2.2
        with:
          ref: gh-pages
          submodules: false

      - name: Clear Directory
        run: rm -rf dev/doxygen

      - name: Get Documentation
        uses: actions/download-artifact@7a1cd3216ca9260cd8022db641d960b1db4d1be4 # v4.0.0
        with:
          name: doxygen
          path: dev/doxygen

      - name: Update Contents
        run: |
          git config user.name Doxygen
          git config user.email doxygen-bot@example.com
          git status
          git add dev/doxygen
          git status
          git commit -m "Update Doxygen documentation for ${{github.sha}}" || echo "No changes to commit"
          git status
          git log -n 1

      - name: Push Updates
        uses: ad-m/github-push-action@77c5b412c50b723d2a4fbc6d71fb5723bcd439aa # v1.0.0
        with:
          github_token: ${{secrets.GITHUB_TOKEN}}
          branch: gh-pages
          force: false
