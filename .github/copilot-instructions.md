# Copilot Instructions

## Naming

- All global variabes begin with a `g_` prefix
- All file scope static variables begin with a `s_` prefix
- Use `lower_case_words` for names of variables and functions with multiple words, except for tests
- Use `UpperCaseWords` for names of types
