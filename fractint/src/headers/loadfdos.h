#if !defined(LOAD_F_DOS_H)

extern int get_video_mode(fractal_info const *info, formula_info_extension_block const *formula_info);
extern double video_mode_aspect_ratio(int width, int height);

#endif
