#if !defined(LOAD_MAP_H)
#define LOAD_MAP_H

extern int validate_luts(const char *filename);
extern int validate_luts(const std::string &filename);
extern void set_color_palette_name(const std::string &filename);

#endif
